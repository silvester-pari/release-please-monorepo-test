{
  "name": "release-please-monorepo-test",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "npm run build -ws && npm publish -w c-test-package-release-please"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.16.0"
  },
  "dependencies": {}
}
